# Sesión 2026-09-10 13:35 — Prueba hola mundo: Fly responde y Pages lo muestra

- **Qué cambió**: se aplicó con `git am` el parche de la prueba hola mundo, sin retoques. `app/src/main.rs` estrena la ruta `GET /holamundo`, que devuelve el texto `holamundo` con `Access-Control-Allow-Origin: *` para que Pages pueda llamarla desde otro origen; `.github/workflows/deploy.yml` añade el paso «Verificar /holamundo», que comprueba cuerpo y cabecera CORS y tumba el run si fallan, y anota la ruta en el resumen del despliegue; `docs/holamundo.html` es la página nueva que hace `fetch` al backend y pinta la respuesta, con build `IN-B1-20260910-003`.
- **SHA**: `652ee06` (`652ee06ffef23080cf41171a2378fb1819bda06c`). Run de Fly [34471770931](https://github.com/npiobject-labs/inversion/actions/runs/34471770931) y run de Pages [34471771025](https://github.com/npiobject-labs/inversion/actions/runs/34471771025), ambos en `success`. En el log del despliegue: `/salud` devuelve el SHA y `/holamundo` responde `holamundo` con CORS.
- **URL para probar**: https://npiobject-labs.github.io/inversion/holamundo.html con build `IN-B1-20260910-003`; backend en https://inversion-npiobject-labs.fly.dev/salud.
- **Drive**: copia de `docs/planificacion/` subida a la carpeta `10437HObdpr75FWMAhUlGzbJf9JKURHIT`, con este resumen en `sesiones/`.
- **Qué falta**: el mock principal sigue siendo la pantalla de fase 0 y no enlaza aún a `holamundo.html`; la máquina de Fly se apaga sin tráfico, así que la primera carga de la página puede tardar unos segundos.
