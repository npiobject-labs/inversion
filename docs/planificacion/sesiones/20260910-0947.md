# Sesión 2026-09-10 09:47 — Enlazar la bitácora desde el mock

- **Qué cambió**: `docs/index.html` sube a `IN-B1-20260910-002` y estrena un enlace visible a «Bitácora del proyecto» separado por una regla; se archivan en `docs/mocks/` el mock anterior (`001-mock0.html`) y el resultante (`002-bitacora.html`). En `docs/bitacora.html`, el `pre` de `.block` gana `padding-right: 5.5rem` para que el botón Copiar deje de tapar el final de la primera línea de cada prompt en móvil.
- **SHA**: `2e9a862` (retoques) y `HEAD` con este cierre. Run de Pages [34451825192](https://github.com/npiobject-labs/inversion/actions/runs/34451825192) en `success`.
- **URL para probar**: https://npiobject-labs.github.io/inversion/ con build `IN-B1-20260910-002`, y la bitácora en https://npiobject-labs.github.io/inversion/bitacora.html.
- **Drive**: copia de `docs/planificacion/` subida a la carpeta `10437HObdpr75FWMAhUlGzbJf9JKURHIT`, con este resumen en `sesiones/`.
- **Qué falta**: el mock sigue siendo la pantalla de fase 0, sin contenido real de producto; `app/` no se ha tocado y Fly nunca se ha desplegado en este proyecto.
